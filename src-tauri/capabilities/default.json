{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window. Tauri v2 denies every core API unless a capability grants it, and with no capability file at all the resolved set is empty - which silently disables the file-drop listener while leaving app-defined commands working, because those are not gated by this system.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
